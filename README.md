# spam_classifier_model
A Flask-based Spam Classifier web app that uses NLP preprocessing (tokenization, stop-word removal, stemming) and a TF-IDF vectorizer with a trained ML model to predict whether a message is Spam or Not Spam. Users enter text in a web form and receive real-time classification results.
